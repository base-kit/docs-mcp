# Day.js 文档路径速查

> 数据源：dayjs 官方文档仓库 `dayjs-website/docs`（154 页，11 个分类）。

## Parse 解析

| 场景 | path |
|------|------|
| **解析总览** | `parse/parse` |
| **当前时间 Now** | `parse/now` |
| **String + Format** | `parse/string-format` |
| 字符串 | `parse/string` |
| Date 对象 | `parse/date` |
| 数组 | `parse/array` |
| 对象 | `parse/object` |
| Unix 时间戳(秒) | `parse/unix-timestamp` |
| Unix 时间戳(毫秒) | `parse/unix-timestamp-milliseconds` |
| UTC 解析 | `parse/utc` |
| 克隆 | `parse/dayjs-clone` |
| 校验 is-valid | `parse/is-valid` |

## Display 显示

| 场景 | path |
|------|------|
| **格式化 format** | `display/format` |
| 显示总览 | `display/display` |
| **diff 差值** | `display/difference` |
| **fromNow 相对时间** | `display/from-now` |
| **to / from** | `display/to` / `display/from` |
| toNow | `display/to-now` |
| **天数 daysInMonth** | `display/days-in-month` |
| 转 String | `display/as-string` |
| 转 JS Date | `display/as-javascript-date` |
| 转 JSON | `display/as-json` |
| 转数组/对象 | `display/as-array` / `display/as-object` |
| ISO 8601 | `display/as-iso-string` |
| Unix 时间戳 | `display/unix-timestamp` |
| Calendar Time | `display/calendar-time` |

## Get + Set 获取/设置

| 场景 | path |
|------|------|
| **get-set 总览** | `get-set/get-set` |
| year / month / date | `get-set/year` / `month` / `date` |
| day 星期 | `get-set/day` |
| hour / minute / second | `get-set/hour` / `minute` / `second` |
| millisecond | `get-set/millisecond` |
| quarter 季度 | `get-set/quarter` |
| **week 周** | `get-set/week` |
| ISO Week | `get-set/iso-week` |
| weekday(本地) | `get-set/weekday` |
| day-of-year | `get-set/day-of-year` |
| max / min | `get-set/max` / `get-set/min` |

## Manipulate 操作

| 场景 | path |
|------|------|
| **add 加** | `manipulate/add` |
| **subtract 减** | `manipulate/subtract` |
| **startOf / endOf** | `manipulate/start-of` / `manipulate/end-of` |
| 本地 local | `manipulate/local` |
| UTC | `manipulate/utc` |
| UTC offset | `manipulate/utc-offset` |

## Query 查询

| 场景 | path |
|------|------|
| 查询总览 | `query/query` |
| **isBefore / isAfter** | `query/is-before` / `query/is-after` |
| **isSame** | `query/is-same` |
| **isBetween** | `query/is-between` |
| isSameOrBefore/After | `query/is-same-or-before` / `is-same-or-after` |
| isLeapYear | `query/is-leap-year` |
| is-a-dayjs | `query/is-a-dayjs` |

## Durations 时长（插件）

| 场景 | path |
|------|------|
| **创建 Duration** | `durations/creating` |
| Duration 总览 | `durations/durations` |
| add / subtract | `durations/add` / `durations/subtract` |
| as 单位转换 | `durations/as` |
| get 获取单位 | `durations/get` |
| days/hours/min/sec | `durations/days` 等 |
| format 格式化 | `durations/format` |
| humanize | `durations/humanize` |
| diff 配合 | `durations/diffing` |

## Timezone 时区（插件）

| 场景 | path |
|------|------|
| **时区总览** | `timezone/timezone` |
| 转换到时区 | `timezone/converting-to-zone` |
| 按时区解析 | `timezone/parsing-in-zone` |
| 猜测用户时区 | `timezone/guessing-user-timezone` |
| 设置默认时区 | `timezone/set-default-timezone` |

## Plugin 插件（38 个）

| 常用插件 | path |
|---------|------|
| **CustomParseFormat** | `plugin/custom-parse-format` |
| **Timezone** | `plugin/timezone` |
| **UTC** | `plugin/utc` |
| **Duration** | `plugin/duration` |
| RelativeTime | `plugin/relative-time` |
| IsBetween | `plugin/is-between` |
| WeekOfYear / IsoWeek | `plugin/week-of-year` / `plugin/iso-week` |
| QuarterOfYear | `plugin/quarter-of-year` |
| MinMax | `plugin/min-max` |
| LocalizedFormat | `plugin/localized-format` |
| AdvancedFormat | `plugin/advanced-format` |
| 插件总览 | `plugin/plugin` |
| NodeJS 加载 | `plugin/loading-into-nodejs` |
| 浏览器加载 | `plugin/loading-into-browser` |

## 其他

| 场景 | path |
|------|------|
| 安装 | `installation/installation` |
| NodeJS / 浏览器 / TS | `installation/node-js` / `browser` / `typescript` |
| i18n 国际化 | `i18n/i18n` |
| 切换 locale | `i18n/changing-locale` |
| 定制（月份/星期名） | `customization/customization` |
| Relative Time 定制 | `customization/relative-time` |
